{{ include('components/html-header.twig') }}
    <body class="{{body_class}}" data-template="base.twig">
        <header class="header mx-auto" >
            {{ include('components/nav.twig') }}
        </header>
        <div class="container mx-auto flex flex-wrap py-6">
            <section id="content" role="main" class="w-full md:w-2/3 flex flex-col items-center px-3">
            {% block content %}
                Empty :(
            {% endblock content %}
            </section>
        </div>
        <footer id="footer" class="bg-white pt-8 w-full">
            {{ include('components/footer.twig') }}
        </footer>
        {{function('wp_footer')}}
    </body>
</html>